# Design

This folder owns repo-level design rules that span pages and components.

- `frontend-design-rules.md` defines the Web Awesome primitive-first policy, documentation expectations, style ownership, and public-site visual boundaries.

Implementation-local design foundations live in `client/src/design/`.
