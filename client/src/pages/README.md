# Pages

This folder owns page-level UI for the public website.

Each page directory should include:

- a page implementation file
- page-owned CSS
- a local `README.md`
- a local PlantUML diagram

Pages may compose reusable components, but reusable component behavior should stay in `client/src/components/`.
